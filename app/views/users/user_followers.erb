<% if @user.id == current_user.id %>
  <h2><%= @user.username %>'s Followers!</h2>
<% else %>
  <h2>Your Followers!</h2>
<% end %>

<div class="followers"><% @user.followers.each do |follower| %>
  <p><a href="/users/<%= follower.id %>"><%= follower.username %></a></p>
  <% end %></div>
